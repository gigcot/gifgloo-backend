services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: gifgloo
      POSTGRES_PASSWORD: gifgloo
      POSTGRES_DB: gifgloo
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
